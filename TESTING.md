# 网站重构测试指南

## 已完成的重构内容

### 1. 设计系统升级（The Verge 风格）
- ✅ 采用深色编辑风格（`#131313` 背景）
- ✅ Jelly Mint (`#3cffd0`) 作为主要强调色
- ✅ Ultraviolet (`#5200ff`) 作为次要强调色
- ✅ 移除所有渐变效果，使用纯色块
- ✅ 圆角系统统一（2/3/4/20/24/30/40px）

### 2. 字体系统
- ✅ 标题字体：Anton（Manuka 替代）
- ✅ 正文字体：Space Grotesk
- ✅ 标签字体：Space Mono（全大写，1.5-1.9px letter-spacing）

### 3. SVG 开屏动画 Session 机制
- ✅ 每次**重新打开浏览器/新标签页**显示动画（4秒）
- ✅ **站内页面跳转**（如 dh.html ↔ index.html）跳过动画
- ✅ 使用 Session Storage（会话级别存储）
- ✅ 添加"跳过动画"按钮
- ✅ 10秒兜底强制隐藏

### 4. 内容丰富
- ✅ Hero Section - 超大号品牌标识
- ✅ StoryStream Timeline - 最新动态时间轴
- ✅ Featured Projects - 彩色块项目展示
- ✅ Skills & Tools - 技能标签云
- ✅ About Me - 个人介绍 + 数据统计
- ✅ Contact - 联系方式（修复 mailto: 协议）

### 5. 代码组织
- ✅ 提取 CSS 到 `styles.css`
- ✅ 提取 JavaScript 到 `script.js`
- ✅ 引入 Google Fonts
- ✅ 响应式设计优化

---

## 测试清单

### 功能测试

#### 1. SVG 开屏动画 Session 机制
**测试步骤：**
1. **首次访问测试**：
   - 打开新浏览器窗口或新标签页
   - 访问 `index.html` → **应该显示4秒动画**
   - 或者点击"跳过动画"按钮立即跳过

2. **站内跳转测试**（不显示动画）：
   - 在主页点击导航栏的"软件"链接 → 跳转到 `dh.html`
   - 在软件页点击"CORALHYMN" Logo → 跳回 `index.html`
   - **应该直接显示主页，不显示动画**

3. **刷新页面测试**（不显示动画）：
   - 在主页按 F5 刷新
   - **应该直接显示主页，不显示动画**

4. **新会话测试**（重新显示动画）：
   - 关闭整个浏览器（所有窗口）
   - 重新打开浏览器，访问网站
   - **应该再次显示动画**

5. **手动清除 Session Storage 测试**：
   - 打开浏览器开发者工具（F12）
   - 切换到 Application/Storage 标签
   - 找到 Session Storage → `coralhymn_animation_shown`
   - 删除该项或清空 Session Storage
   - 刷新页面 → **应该再次显示动画**

**预期结果：**
- ✅ 新会话/新浏览器窗口：显示动画
- ✅ 站内页面跳转：跳过动画
- ✅ 同一标签页刷新：跳过动画
- ✅ 关闭浏览器后重新打开：显示动画

#### 2. 导航栏
**测试步骤：**
1. 桌面端：导航链接应水平排列
2. 移动端（<768px）：应显示"菜单 ☰"按钮
3. 点击菜单按钮：导航链接应展开/收起
4. 点击任意锚点链接：应平滑滚动到对应区域
5. 滚动页面：当前区域对应的导航链接应高亮（mint 下划线）

#### 3. StoryStream 时间轴
**测试步骤：**
1. 检查时间轴左侧的紫色竖线
2. 每个条目应有时间戳（Mono 字体，全大写）
3. 悬停卡片时，标题应变为 Deep Link Blue (`#3860be`)

#### 4. 彩色块项目卡片
**测试步骤：**
1. 三张卡片应分别显示 Mint、Purple、Yellow 背景
2. 卡片上的文字颜色应对比清晰
3. 悬停时标题颜色变化

#### 5. 技能标签
**测试步骤：**
1. 四个分类：游戏开发、编程语言、设计工具、其他
2. 每个标签应为 pill 形状（圆角20px）
3. 标签边框为白色，背景为黑色

#### 6. 软件列表页 (dh.html)
**测试步骤：**
1. 打开 `dh.html`
2. 检查筛选标签（全部/工具/游戏/插件）
3. 点击不同筛选标签，列表应相应过滤
4. 每个软件条目应包含：图标、名称、描述、日期、标签、操作按钮

#### 7. 响应式设计
**测试步骤：**
1. 使用浏览器开发者工具的 Device Toolbar
2. 测试以下断点：
   - Desktop (≥1280px)
   - Tablet (768-1023px)
   - Mobile (<768px)
   - Small Mobile (<480px)
3. 确保所有内容在各断点下正常显示

#### 8. 链接和按钮
**测试步骤：**
1. 点击"进入互动作品集" → 应跳转到 `./godot/index.html`
2. 点击"访问博客" → 应打开 `https://cc00.top/`
3. 点击邮箱链接 → 应打开邮件客户端（mailto: 协议）
4. 点击社交媒体图标 → 应在新标签页打开对应平台
5. 所有外部链接应有 `target="_blank"` 和 `rel="noopener"`

---

## 已知问题与注意事项

### 1. Godot 目录
- **状态**：本地不存在 `godot/` 目录
- **说明**：你提到服务器上存在该目录，因此保留链接
- **建议**：如果本地测试需要，可以创建一个占位页面

### 2. 外部图片依赖
- SVG Logo: `https://img.h440.top/PicGo/coralhymn2-587.svg`
- 备案图标: `https://img.h440.top/PicGo/20250825114220920.png`
- **风险**：CDN 失效会导致图片丢失
- **建议**：下载图片到本地 `images/` 目录

### 3. 字体加载
- 使用 Google Fonts CDN
- **风险**：国内访问可能较慢
- **建议**：可以考虑使用国内镜像（如中科大镜像）

### 4. Cookie 隐私合规
- 当前实现不包含 Cookie 同意横幅
- **注意**：如果面向欧盟用户，需要添加 GDPR 合规提示

---

## 浏览器兼容性

### 已测试浏览器
- ✅ Chrome 120+
- ✅ Firefox 121+
- ✅ Edge 120+
- ⚠️ Safari（未测试，建议使用 `-webkit-` 前缀）

### CSS 特性
- CSS Variables（`:root`）- 现代浏览器支持良好
- Flexbox - 广泛支持
- Grid - 现代浏览器支持良好
- `backdrop-filter` - 已移除（不符合设计规范）

---

## 性能优化建议

### 当前状态
- 无图片懒加载
- 无代码压缩
- 无缓存策略

### 可选优化
1. **图片懒加载**：为 `<img>` 添加 `loading="lazy"`
2. **代码压缩**：使用 terser 压缩 JS，cssnano 压缩 CSS
3. **Service Worker**：添加离线缓存支持
4. **字体预加载**：在 `<head>` 添加 `<link rel="preload">`

---

## 下一步行动

### 立即可做
1. 在多个浏览器中测试功能
2. 验证 Cookie 逻辑是否正常工作
3. 检查响应式布局在各断点的表现

### 后续优化
1. 添加 SEO 元标签（keywords, author 等）
2. 添加结构化数据（JSON-LD）
3. 集成分析工具（Umami/Matomo）
4. 创建 PWA manifest.json
5. 添加多语言支持（i18n）

---

## 故障排除

### 问题：动画一直显示，不消失
**解决方案：**
1. 打开浏览器 Console（F12）
2. 检查是否有 JavaScript 错误
3. 手动运行 `skipLoading()` 函数
4. 清除 Cookie 后刷新页面

### 问题：样式未加载
**解决方案：**
1. 检查 `styles.css` 文件是否存在
2. 检查浏览器 Network 标签，确认 CSS 文件成功加载
3. 确认文件路径正确（相对路径 `./styles.css`）

### 问题：字体显示异常
**解决方案：**
1. 检查网络连接（Google Fonts 需要联网）
2. 检查浏览器是否阻止了外部字体加载
3. 备用字体会自动生效（Impact, Helvetica, Arial）

### 问题：移动端菜单不工作
**解决方案：**
1. 确认 `script.js` 文件已加载
2. 检查 Console 是否有 JavaScript 错误
3. 确认窗口宽度小于 768px

---

## 联系支持

如有问题，请检查：
1. 浏览器 Console 的错误信息
2. Network 标签的资源加载状态
3. Application 标签的 Cookie 存储

祝测试顺利！🎉
